apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minio-data
  namespace: market-siparis
  labels:
    app.kubernetes.io/name: market-minio
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
